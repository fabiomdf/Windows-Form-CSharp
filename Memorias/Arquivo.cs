using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Memorias
{
    public class Arquivo
    {
        public Arquivo(NandFFS fs)
        {
        }
        //public int Open(string path);
        //public int Seek(UInt32 pos);
        //public UInt32 Read(UInt32 offset, UInt32 size, byte[] buf);
        //public UInt32 Read(UInt32 size, byte[] buf);
        //public UInt32 GetSize();

        //protected UInt32 objectId;
        //protected NandFFS[] fs;
        //protected UInt32 ReadPtr;
        //protected UInt32 size;
    }
}